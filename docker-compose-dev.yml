version: '3'
services: 
  react-app:
    build:
      context: . # It essentially tells Docker what files and directories to send to the Docker daemon during the build process.
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes: 
      - /app/node_modules
      - .:/app